// game_state.c - defines the gamestate struct

#include "game_state.h"
#include <string.h>         // For memset()

/* Initializes the GameState Struct
Function sets all bytes in the struct to zero then manually assigns default values
Ensures clean memory and avoids garbage values */
void GameState_init(GameState* state) {
    // Zero out the entire structs memory
    
}